# knockout.observableHashtable

> Observable hashtable (dictionary) for use with knockout.js

## API


* add(key: K, value: V);
* clear();
* containsKey(key: K);
* remove(key: K);
* destroy(key: K);
* destroyAll();
* keys(): K[];
* values(): V[];